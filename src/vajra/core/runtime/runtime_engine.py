"""
Runtime Engine

The Runtime Engine executes the plans
created by the Planner.

Future responsibilities:
- Execute real system actions
- Communicate with capabilities
- Handle failures
- Track execution history
"""


class RuntimeEngine:
    """
    Responsible for executing
    Vajra execution plans.
    """

    def execute(self, plan):
        """
        Executes every task inside a plan.

        Example:

        Input:
        [
            "Understand goal",
            "Collect information"
        ]

        Output:
        Execution result
        """

        # Store execution results
        results = []

        # Process each task sequentially
        for task in plan:

            # Future versions will replace this
            # with actual capability execution.
            result = f"Completed task: {task}"

            # Save completed task result
            results.append(result)

        # Return complete execution history
        return results

    """
Runtime Engine

The Runtime Engine is responsible for executing plans
generated by the Planner.

It delegates real work to registered capabilities.
"""

# Import the Capability Registry.
from vajra.capabilities.registry import CapabilityRegistry


class RuntimeEngine:
    """
    Executes execution plans.
    """

    def __init__(self):
        """
        Create a Capability Registry.
        """
        self.registry = CapabilityRegistry()

    def execute(self, plan):
        """
        Execute each task in the plan.

        Parameters:
            plan (list): List of planned tasks.

        Returns:
            list: Execution results.
        """

        results = []

        for task in plan:

            # Current implementation:
            # If a task involves creating something,
            # use the File System capability.
            if "create" in task.lower():

                filesystem = self.registry.get_capability("filesystem")

                result = filesystem.create_directory("vajra_output")

                results.append(result)

            else:

                results.append(f"Completed task: {task}")

        return results